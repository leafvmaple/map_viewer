/// <reference types="vite/client" />

/** App version (package.json), injected by Vite at build time. */
declare const __APP_VERSION__: string;
/** ISO build timestamp, injected by Vite at build time. */
declare const __BUILD_DATE__: string;
