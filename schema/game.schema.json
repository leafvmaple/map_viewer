{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/leafvmaple/map_viewer/schema/game.schema.json",
  "title": "Game Map Viewer — game.json",
  "description": "Contract for a single game's map configuration, produced by nes_decoder and consumed by the map_viewer web app. See CONTRACT.md.",
  "type": "object",
  "required": ["id", "name", "defaultMap", "maps"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z0-9_]+$",
      "description": "Stable game identifier. Must match the registry entry and the res/{id}/ folder name."
    },
    "version": {
      "type": "integer",
      "minimum": 1,
      "description": "Optional contract version this file was produced against (currently 1). Lets a future breaking change be detected instead of silently misread."
    },
    "name": { "$ref": "#/$defs/localizedString" },
    "defaultMap": {
      "type": "string",
      "minLength": 1,
      "description": "Key in `maps` shown first and treated as the navigation root (usually \"world_map\")."
    },
    "maps": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": { "pattern": "^[A-Za-z0-9_]+$" },
      "additionalProperties": { "$ref": "#/$defs/map" }
    },
    "export": {
      "type": "object",
      "description": "Provenance stamp written automatically at export time — identifies which export (and which decoder iteration) produced this data. Shown in the viewer's build-identifier chip.",
      "required": ["exportedAt"],
      "additionalProperties": false,
      "properties": {
        "exportedAt": {
          "type": "string",
          "minLength": 1,
          "description": "ISO 8601 timestamp of the export."
        },
        "producer": {
          "type": "string",
          "description": "Producing tool + its git sha, e.g. \"nes_decoder@4b3941b\"."
        },
        "rom": {
          "type": "string",
          "description": "Short hash of the source ROM, e.g. \"sha1:9f2a01c4\"."
        }
      }
    },
    "poiKinds": {
      "type": "object",
      "description": "Optional display metadata for this game's POI kinds — legend name + glyph ship with the data, so new kinds need no viewer update. Kinds absent here fall back to the viewer's built-ins.",
      "propertyNames": { "pattern": "^[a-z0-9_]+$" },
      "additionalProperties": {
        "type": "object",
        "required": ["name"],
        "additionalProperties": false,
        "properties": {
          "name": { "$ref": "#/$defs/localizedString" },
          "glyph": { "type": "string", "minLength": 1, "description": "Legend/list glyph (an emoji)." }
        }
      }
    }
  },
  "$defs": {
    "localizedString": {
      "type": "object",
      "description": "Display text keyed by language code. At least one entry required; `en` is the fallback.",
      "minProperties": 1,
      "properties": {
        "en": { "type": "string" },
        "zh": { "type": "string" },
        "ja": { "type": "string" }
      },
      "additionalProperties": { "type": "string" }
    },
    "map": {
      "type": "object",
      "required": ["name", "type", "triggers"],
      "additionalProperties": false,
      "properties": {
        "name": { "$ref": "#/$defs/localizedString" },
        "type": {
          "enum": ["image", "tiles"],
          "description": "Render mode: a single image, or a pre-rendered tile pyramid (tilesPath)."
        },
        "image": {
          "type": "string",
          "minLength": 1,
          "description": "Path to the map image, relative to this game.json's folder (res/{id}/)."
        },
        "thumbnail": {
          "type": "string",
          "description": "Optional low-res placeholder for progressive loading. Only used when `width` and `height` are also present."
        },
        "width": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "description": "Optional intrinsic pixel width of `image`. Lets the viewer skip probing the full image."
        },
        "height": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "description": "Optional intrinsic pixel height of `image`."
        },
        "tileSize": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "description": "Tile size in pixels; drives the grid overlay and the editor's snap-to-grid. Typically 16."
        },
        "floorGroup": {
          "type": "string",
          "minLength": 1,
          "description": "Optional building/dungeon key: maps sharing it are floors of one place and get an in-map floor switcher. Must be STABLE across re-exports (derive from map table facts, not enumeration order)."
        },
        "floor": {
          "type": "integer",
          "description": "Floor number within `floorGroup` (1 = ground, negative = basement: -1 renders B1F). Drives switcher order and labels."
        },
        "maxZoom": { "type": "number" },
        "tilesPath": { "type": "string", "description": "Tile URL template for type=tiles, relative to the game res dir (e.g. world_tiles/{z}/{x}_{y}.png); latlng space stays full-res pixels." },
        "minNativeZoom": { "type": "number", "description": "Lowest pre-rendered pyramid level for type=tiles (default -5)." },
        "triggers": {
          "type": "array",
          "items": { "$ref": "#/$defs/trigger" }
        },
        "jumps": {
          "type": "array",
          "description": "Non-spatial map jumps shown in the sidebar when exact trigger coordinates are unknown.",
          "items": { "$ref": "#/$defs/jump" }
        },
        "pois": {
          "type": "array",
          "description": "Points of interest (treasure chests, etc.) shown as icons.",
          "items": { "$ref": "#/$defs/poi" }
        },
        "events": {
          "type": "array",
          "description": "Event-driven tile changes (e.g. a wall opening); each is a toggleable overlay.",
          "items": { "$ref": "#/$defs/event" }
        }
      }
    },
    "jump": {
      "type": "object",
      "description": "A map-to-map navigation item without a clickable spatial bounds rectangle.",
      "required": ["target"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "target": {
          "type": "string",
          "minLength": 1,
          "description": "Key of the destination map within this same game's `maps`."
        },
        "kind": { "type": "string", "description": "Optional jump semantics, e.g. warp, door, shortcut." },
        "label": { "$ref": "#/$defs/localizedString" },
        "focus": {
          "type": "array",
          "description": "Optional target pixel focus [x, y], full-resolution image space.",
          "minItems": 2,
          "maxItems": 2,
          "items": { "type": "number" }
        }
      }
    },
    "event": {
      "type": "object",
      "description": "A terrain change a map applies when a story event fires; toggled by a checkbox in the viewer.",
      "required": ["id", "bounds", "overlay"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "minLength": 1, "description": "Event id (hex) within the map." },
        "label": { "$ref": "#/$defs/localizedString" },
        "bounds": { "$ref": "#/$defs/bounds" },
        "overlay": {
          "type": "string",
          "minLength": 1,
          "description": "Path to the changed-tiles overlay image, relative to this game.json's folder."
        },
        "tiles": { "type": "integer", "description": "Number of tiles this event changes." }
      }
    },
    "trigger": {
      "type": "object",
      "description": "A clickable zone that navigates to another map (a door / exit / warp).",
      "required": ["id", "bounds", "target", "label"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Unique within the map. Convention: {mapId}_t{NN}."
        },
        "bounds": { "$ref": "#/$defs/bounds" },
        "target": {
          "type": "string",
          "minLength": 1,
          "description": "Key of the destination map within this same game's `maps`, or __return__ for a return-stack exit."
        },
        "kind": {
          "type": "string",
          "description": "Optional trigger semantics, e.g. door, exit, or return."
        },
        "returnTargets": {
          "type": "array",
          "description": "Candidate source maps for a return-stack exit when no navigation context exists.",
          "items": { "$ref": "#/$defs/returnTarget" }
        },
        "label": { "$ref": "#/$defs/localizedString" }
      }
    },
    "returnTarget": {
      "type": "object",
      "required": ["target"],
      "additionalProperties": false,
      "properties": {
        "target": { "type": "string", "minLength": 1 },
        "focus": {
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "type": "number" }
        },
        "label": { "$ref": "#/$defs/localizedString" }
      }
    },
    "poi": {
      "type": "object",
      "description": "A point of interest on a map (e.g. a treasure chest).",
      "required": ["id", "kind", "pos"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "kind": { "type": "string", "description": "POI category, e.g. \"treasure\"." },
        "pos": {
          "type": "array",
          "description": "Pixel position [x, y] (top-left of the tile), full-resolution image space.",
          "minItems": 2,
          "maxItems": 2,
          "items": { "type": "number" }
        },
        "label": { "$ref": "#/$defs/localizedString" },
        "item": { "type": "string", "description": "Optional item id/hex for treasure POIs." },
        "icon": {
          "type": "string",
          "minLength": 1,
          "description": "Optional sprite image path (relative to the game's res dir); rendered as the marker instead of a glyph, on every map."
        },
        "iconSize": {
          "type": "array",
          "description": "Native pixel size [w, h] of `icon` (defaults to [16, 32]).",
          "minItems": 2,
          "maxItems": 2,
          "items": { "type": "integer", "minimum": 1 }
        },
        "itemIcon": {
          "type": "string",
          "minLength": 1,
          "description": "Optional item mini-icon: relative image path for ROM bag icons, or emoji:<glyph> when no item icon graphics exist. Shown in the tooltip, treasure panel and checklist — not as a map marker."
        },
        "items": {
          "type": "array",
          "description": "Explicit contents for a POI that yields multiple items. Each item can carry its own name, raw id, and mini-icon.",
          "items": { "$ref": "#/$defs/poiItem" }
        },
        "hidden": {
          "type": "boolean",
          "description": "True when the collectible has NO visible sprite in the rendered map image (buried / Itemfinder-style items). Only these get an attention glyph in the viewer; visible chests rely on their baked-in sprite + a hover zone."
        },
        "party": {
          "type": "array",
          "description": "Optional structured battle party (a trainer's Pokémon, an enemy general's army…); the viewer renders it as an icon/name/badge card in the tooltip and makes the POI markable. Full roster, in battle order — do not truncate.",
          "items": { "$ref": "#/$defs/partyMember" }
        },
        "reward": {
          "$ref": "#/$defs/localizedString",
          "description": "Optional defeat reward (prize money, spoils…), pre-formatted (e.g. \"¥624\"). Shown dimmed in the party-card header."
        }
      }
    },
    "poiItem": {
      "type": "object",
      "required": ["name"],
      "additionalProperties": false,
      "properties": {
        "name": { "$ref": "#/$defs/localizedString" },
        "item": { "type": "string", "description": "Optional item id/hex for this contained item." },
        "itemIcon": {
          "type": "string",
          "minLength": 1,
          "description": "Optional item mini-icon for this contained item: image path or emoji:<glyph>."
        }
      }
    },
    "partyMember": {
      "type": "object",
      "description": "One member of a POI's battle party, rendered as an icon · name · badge row.",
      "required": ["name"],
      "additionalProperties": false,
      "properties": {
        "name": { "$ref": "#/$defs/localizedString" },
        "value": {
          "type": "integer",
          "minimum": 0,
          "description": "Numeric badge, right-aligned (a level, a troop count…). Omit for a name-only row."
        },
        "unit": {
          "$ref": "#/$defs/localizedString",
          "description": "Localized badge prefix shown dimmed before `value`. Default \"Lv\" — e.g. {\"zh\": \"兵\"} renders 兵12000."
        },
        "id": {
          "type": "integer",
          "minimum": 0,
          "description": "Optional game-internal id of this member (species / general id), for debugging/stability."
        },
        "icon": {
          "type": "string",
          "minLength": 1,
          "description": "Optional mini-icon path (relative to the game's res dir). Missing icon → the viewer shows a placeholder; the row layout is unaffected."
        }
      }
    },
    "bounds": {
      "type": "array",
      "description": "Axis-aligned rectangle in FULL-RESOLUTION image pixels: [[x1, y1], [x2, y2]], top-left origin.",
      "minItems": 2,
      "maxItems": 2,
      "items": {
        "type": "array",
        "minItems": 2,
        "maxItems": 2,
        "items": { "type": "number" }
      }
    }
  }
}
